import {
  GET_USERS_REQUEST,
  GET_USERS_SUCCESS,
  GET_USERS_FAIL,
} from "../constansts/userConstants";
export const userReducer = (state = { users: [] }, actions) => {
  switch (actions.type) {
    case:GET_USERS_REQUEST:
  }
};
